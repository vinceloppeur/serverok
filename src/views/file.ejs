<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Serverok | <%= filename %>
  </title>
  <link rel="stylesheet" href="/styles/index.css">
  <link rel="stylesheet" href="/styles/file.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body style="display: flex; flex-direction: column; height: 100vh;">
  <%- include("templates/navbar.ejs") %>
  <main>
    <div style="width: 100%;">
      <div class="file">
        <h3>
          <%= filename %>
        </h3>
        <div>
          <% if (!root) { %>
            <span><%= filepath %></span>
            <span>|</span>
          <% } %>
          <span>
            <%= filesize %>KB
          </span>
        </div>
      </div>
      <% if (download) { %>
        <a href="/download" class="navigation--download">Download</a>
      <% } else { %>
        <form method="post" class="container--navigation" style="margin-top: 1rem;">
          <% if (downloadUrl) { %>
            <div class="url">
              <a href="<%= downloadUrl %>" target="_blank"><i class="fa-regular fa-copy" style="margin-right: 0.5rem;"></i>
                <%= downloadUrl %>
              </a>
            </div>
          <% } %>
          
          <div style="display: flex; align-items: center; gap: 1rem;">
            <% if (!root) { %>
              <a href="../" class="navigation-back">Back</a>
            <% } %>
            <input class="navigation-share" type="submit" value="Share">
          </div>
        </form>
      <% } %>
    </div>
  </main>
</body>

</html>